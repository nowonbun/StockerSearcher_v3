# 2026-07-26: JP/KR 주식 추천 모델 검토 및 필터 적용 기록

## 결론

- 현재 Transformer 모델은 실제 비교 실험에서 가장 좋은 결과를 냈으므로 운영 기준 모델로 유지한다.
- 현재 모델을 LightGBM으로 교체하지 않는다. LightGBM은 향후 성능 하락 여부를 확인하는 비교 기준선으로만 사용한다.
- 추천 단계에서는 폭락 후 반등 후보를 그대로 추천하지 않도록 추세, 일목균형표, 유동성, 확률 필터를 적용한다.
- 다음 모델 개선은 기존 구조를 확장한 `model_*_v3` 실험 모델로 진행하고, 기존 모델보다 시간 분리 검증 성과가 좋을 때만 교체한다.

## 확인된 현상과 원인

### 2026-07-24 JP 일봉 추천

- `stock_predict_jp`에는 2026-07-24 기준 정확히 50개 종목이 저장되어 있었다.
- 저장 확률 범위는 약 0.4714~0.7205였고, 실행 모델명은 `model_jp_v2.pt`였다.
- 상위 50개 종목의 평균 위치는 60일선 대비 약 -30.1%, 20일선 대비 약 -14.6%였다.
- 상위 후보에는 저유동성 종목도 포함됐다. 예를 들어 462A와 8783의 당일 거래대금은 각각 약 3,038만, 7,412만이었다.

### 구조적 원인

1. V2 입력 특성은 하단밴드 대비 위치, 밴드 위치, 20일 낙폭, HA 일수익률, HA 몸통 비율의 5개다. 시장 국면, 일목균형표, 20/60 이동평균 관계는 포함하지 않는다.
2. 일부 낙폭 특성은 하한 -0.5로 clip된다. 극단적 하락과 일반 조정이 같은 입력값으로 뭉개질 수 있다.
3. 학습에는 최근 5기간 거래대금 합계 10억 이상 필터가 있었지만, 기존 추론에는 같은 유동성 필터가 없었다.
4. 기존 추론은 최소 확률 기본값 없이 `top_k`를 잘라 저장하므로, 추천할 종목이 부족한 날에도 JP 일봉 50개가 저장될 수 있었다.
5. 최신 JP V2 학습 로그의 최고 AP는 약 0.319이고 검증 양성 비율은 약 0.272였다. 0.5 임계값 기준 정밀도는 약 40%, 재현율은 약 1%였다. 일반 accuracy는 추천 정확도를 대표하지 않는다. 모든 종목을 비상승으로 예측해도 양성 비율 때문에 약 72.8% accuracy가 가능하다.

## 적용한 추론 필터

공통 구현 파일은 `src/predict/common.py`다. 다음 필터를 기본 활성화했다.

- 최소 확률: `0.25` (기존 V2의 검증 양성 비율 약 0.272와 추세 통과 종목의 예측값을 기준으로 한 임시값)
- 유동성: 최근 5기간 거래대금 합계 `1,000,000,000` 이상
- 추세: `20MA > 60MA` 또는 종가가 26·52 구름대 상단보다 높음
- 필터 통과 종목이 없으면 추천 결과도 없다.

일목균형표의 현재 구름대는 선행 26기간 이동을 반영한다. 따라서 52기간 Span B와 26기간 이동을 계산하기 위해 최소 78기간의 고가/저가 이력을 조회한다.

적용 대상은 다음 네 개의 추론 엔트리포인트다.

- `src/predict/predict_jp_v2.py`
- `src/predict/predict_kr_v2.py`
- `src/predict/predict_week_jp_v2.py`
- `src/predict/predict_week_kr_v2.py`

추론 SQL에서 숫자로 시작하는 이동평균 컬럼을 인용하지 않던 문제와 PostgreSQL 연결 import 문제도 수정했다.

## 검증 결과

- `tests/test_prediction_filters.py`를 추가했다.
- 구름대 상단 계산, 구름대 하단 탈락, 20/60 역배열 탈락, 유동성 부족 탈락을 테스트한다.
- 실행 명령: `PYTHONPATH=src python -m unittest discover -s tests`
- 결과: 9개 테스트 통과.
- 2026-07-24, JP 코드 4004 단일 추론을 `--save-db` 없이 실행했고, 필터 불충족으로 결과가 출력되지 않는 것을 확인했다.
- 이 검증 과정에서 DB 쓰기는 수행하지 않았다.

## 모델 학습 방향

### 현재 모델

- 프로젝트는 `StockerSearcher_v3`다.
- 현재 모델 구현 및 가중치 파일명은 `model_*_v2.py`, `model_*_v2.pt`다.
- 실제 실험에서 현재 Transformer가 가장 좋았다는 사용자 확인을 운영 기준으로 삼는다. 이 성능 우위는 이번 기록에서 독립적으로 재현하지 않았다.

### 다음 실험 모델

- 프로젝트 버전을 바꾸지 않고, 다음 도전 모델을 `model_*_v3.py`, `model_*_v3.pt`로 만든다.
- 기존 V2 특성은 유지하고 다음 특성을 추가한다.
  - 구름대 상단 대비 종가
  - 20MA/60MA 비율
  - 20MA와 60MA의 기울기
  - 구름대 두께와 방향
  - 유동성 및 변동성 특성
- 학습/검증 필터는 예측 기준일 당시 데이터만으로 계산한다. 20기간 후 수익률은 라벨에만 사용해 미래정보 누수를 막는다.
- JP/KR, 일봉/주봉은 분리 학습하고, 상승장·횡보장·하락장 구간을 분리 평가한다.
- 후보 모델 교체 조건은 롤링 시간 분리 검증에서 기존 V2보다 `Precision@10`, `Precision@20`, 20기간 후 수익률, 하락장 성적 및 최대낙폭을 함께 개선하는 것이다.

### 지표 원칙

- 일반 accuracy를 모델 교체 지표로 사용하지 않는다.
- 매수추천 품질은 `Precision@10`, `Precision@20`, 필터 통과 종목의 평균/중앙 수익률, 최대낙폭으로 평가한다.

## 오픈 모델 조사 메모

- 범용 시계열 파운데이션 모델로 `amazon/chronos-2`, `google/timesfm-2.5-200m-transformers`, `Salesforce/moirai-2.0-R-small`을 조사했다.
- 이 모델들이 일본/한국 개별주식에서 현재 모델보다 좋다는 것은 검증되지 않았다.
- `Chronos-2`는 다변량·공변량 입력과 분위수 예측을 지원하므로 별도 벤치마크 후보가 될 수 있다.
- `TimesFM`은 별도 벤치마크 후보가 될 수 있다.
- `Moirai-2.0-R-small`은 CC-BY-NC-4.0 라이선스이므로 상업적 사용 계획이 있으면 제외한다.
- 뉴스/공시 감성 입력이 없는 현재 데이터에는 FinGPT 계열을 가격 예측 대체 모델로 사용하지 않는다.
- 직접 학습 모델 중 LightGBM/CatBoost와 PatchTST는 비교 실험 후보지만, 현재 Transformer를 대체하는 기본 선택은 아니다.

## 근거 파일

- `src/create_model/model_jp_v2.py`
- `src/create_model/log/model_jp_v2_20260720_184834.log`
- `src/predict/common.py`
- `src/predict/predict_jp_v2.py`
- `src/predict/predict_kr_v2.py`
- `src/predict/predict_week_jp_v2.py`
- `src/predict/predict_week_kr_v2.py`
- `tests/test_prediction_filters.py`
